# Nexus Bank - Electron App

Modern banking application built with React, Firebase, and Electron.

## Setup

1.  Clone the repository.
2.  Run `npm install`.
3.  Run `npm run dev` for development.
4.  Run `npm run electron:build` to build the executable.
